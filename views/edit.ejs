<%-include('./partials/header')%>
<%-include('./partials/navEdit')%>

<div class="container py-4">

    <form action="/edit/<%=coupons._id%>" method="POST">
        <div class="mb-3">
            <label for="address" class="form-label">Popraw Adres 🏚️</label>
            <input value="<%=coupons.address%>" type="text" class="form-control" name="address" required>
        </div>

        <div class="mb-3">
            <label for="desc" class="form-label">Popraw Opis ✏️</label>
            <textarea type="text" class="form-control" name="desc" required><%=coupons.desc%></textarea>
        </div>

        <button type="submit" class="btn btn-success">Zapisz Zmiany</button>
    </form>
</div>
<%-include('./partials/footer')%>